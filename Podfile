# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

target 'PunchCardDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'PunchCard', :path => './'
  #pod 'RealmSwift'
  
  # Pods for PunchCardDemo
  
  target 'PunchCardDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
end
